version: "3.9"

services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bluey
      POSTGRES_PASSWORD: bluey
      POSTGRES_DB: bluey
    volumes:
      - bluey_pgdata:/var/lib/postgresql/data

volumes:
  bluey_pgdata:
